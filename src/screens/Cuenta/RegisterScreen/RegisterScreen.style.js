import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    img: {
        resizeMode: 'contain',
        width: '100%',
        height: 200,
        marginTop: 20
    },
    content:{
        marginHorizontal:40,
    }
});